{
  "name": "namespace-context",
  "description": "Mixin/utility for Assemble, Verb, or Template that makes it easy to add namespaced variables to the context, like `page` or `post`. This allows you to use `{{page.title}}` instead of `{{title}}` to get the current page's title. Can be used with verb, assemble v0.6.0, or any application based on jonschlinkert/template.",
  "version": "0.1.0",
  "homepage": "https://github.com/jonschlinkert/namespace-context",
  "author": "Jon Schlinkert (https://github.com/jonschlinkert)",
  "repository": "jonschlinkert/namespace-context",
  "bugs": {
    "url": "https://github.com/jonschlinkert/namespace-context/issues"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "main": "index.js",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "mocha"
  },
  "dependencies": {
    "clone-deep": "^0.2.2",
    "is-buffer": "^1.0.2"
  },
  "devDependencies": {
    "async": "^1.4.2",
    "engine-handlebars": "^0.6.1",
    "mixin-deep": "^1.1.3",
    "mocha": "*",
    "should": "*",
    "templates": "^0.1.8"
  },
  "keywords": [
    "expand",
    "process"
  ],
  "verb": {
    "related": {
      "list": [
        "assemble",
        "verb",
        "template",
        "expand"
      ]
    }
  }
}